{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Billete list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Codgo</th>
                <th>Asiento</th>
                <th>Fecha</th>
                <th>Estado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('billete_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.codgo }}</td>
                <td>{{ entity.asiento }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.estado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('billete_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('billete_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('billete_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
